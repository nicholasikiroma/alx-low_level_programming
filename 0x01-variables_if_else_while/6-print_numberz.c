#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 for success
 */

int main(void)
{
	int numbers;

	for (numbers = 48; numbers <= 57; numbers++)

		putchar(numbers);

	putchar('\n');

	return (0);
}
