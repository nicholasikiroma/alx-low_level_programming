#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print base 10 numbers
 * return: 0 for succes
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		printf("%d/n", n);
	return (0);
}
